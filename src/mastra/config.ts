// Mastra configuration temporarily disabled due to TypeScript interface issues
// We're using API routes directly instead which works perfectly

export const mastra = {
  getAgent: (_name: string) => null, // Placeholder - using API routes instead
  agents: {}
};

export default mastra;
